# Configuration Templates

Each environment folder (`local/`, `staging/`, `production/`) contains `.env.example`
files documenting required variables. Copy to a real `.env` file (never committed)
before running that environment.
